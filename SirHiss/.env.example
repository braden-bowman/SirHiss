# SirHiss Trading Bot Platform Environment Configuration
# Copy this file to .env and update with your actual values

# Database Configuration
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=generate_strong_password_here
POSTGRES_DB=your_database_name

# Application Security (CRITICAL: Generate your own secure values)
SECRET_KEY=generate-your-own-32-char-secret-key
ENCRYPTION_SALT=generate-your-own-salt-string
SIRHISS_API_KEY=generate-your-own-api-key-for-access
ENVIRONMENT=production

# Trading Platform Credentials (Configure through the web interface)
# These are managed through the secure credentials system in the app
# ROBINHOOD_USERNAME=configured_via_web_interface
# ROBINHOOD_PASSWORD=configured_via_web_interface

# Redis Configuration (usually don't need to change)
REDIS_URL=redis://redis:6379

# API URLs (usually don't need to change for Docker setup)
REACT_APP_API_URL=http://localhost:9002/api/v1
REACT_APP_WS_URL=ws://localhost:9002

# Frontend API Key (should match SIRHISS_API_KEY above)
REACT_APP_SIRHISS_API_KEY=generate-your-own-api-key-for-access

# Bot Configuration
MAX_CONCURRENT_BOTS=10
BOT_EXECUTION_INTERVAL=60

# Logging Level
LOG_LEVEL=INFO